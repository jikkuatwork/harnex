# harnex 0.9.0 - release verification

- **Released:** 2026-08-03 | 01:16 AM | IST
- **Commit:** `abcbc1b`
- **Tag:** `v0.9.0`
- **Headline changes:** #63 single tracked telemetry stream: exactly one v2
  `dispatch_start` + one rich v2 `dispatch_end`; explicit-only
  `--summary-out` mirror; Codex price-table cost with effective
  model/service-tier/context-band provenance; harness-owned attempt linkage;
  repo phase allowlist; bounded events/output retention; packaged telemetry and
  configuration docs. Claude usage producer (#58) is explicitly deferred.
- **Verified against:** published and locally installed
  `harnex 0.9.0 (2026-08-03)`.

## Functional matrix

| # | Test | Result | Notes |
|---|------|--------|-------|
| 1 | Full release suite at candidate HEAD | **pass** | `ruby -Ilib -Itest -e 'Dir["test/**/*_test.rb"].each { |f| require_relative f }'`: 638 runs, 2726 assertions, 0 failures, 0 errors, 2 env-gated skips. Syntax checks passed for pricing/config/retention/session/doctor. |
| 2 | Packaged-doc contract | **pass** | `test/harnex/gemspec_test.rb`: core `README`/`GUIDE`/`TECHNICAL`, `docs/{configuration,dispatch-telemetry,events}.md`, and agent guides are in `spec.files`; local Markdown links passed across 15 public files. Published gem contents include the new `docs/*.md` payload and bundled agent guides. |
| 3 | Real source Codex v2/cost/proof smoke | **pass after environment correction** | First attempt `cx-smoke-090` intentionally failed closed in 0s because the invoking shell lacked `AZURE_OPENAI_API_KEY`; it still wrote one v2 start + one rich failed end with typed `task_failed`/`report_invalid`. No retry was launched until events identified the environment cause. Attempt `cx-smoke-090b` inherited the existing tmux provider environment without printing/copying credentials and passed: exactly 2 rows, strict report accepted, effective `gpt-5.5` + `flex`, context peak 11,751 (<272K), `usage.cost_usd=0.073761`, `cost_source=price_table`, `cost_price_as_of=2026-08-03`, attempts 1/1/0. Bounded source evidence: `koder/releases/0.9.0-source-smoke.json` (SHA-256 `0cc9bc0a7fd5a61c5e2c83b5de6c811f1d5e98d77fe5016e0396650f649612c3`). |
| 4 | Explicit mirror smoke | **pass** | Source binary generic dispatch wrote one canonical start + end and one mirror end; parsed mirror object equaled the tracked v2 end exactly. |
| 5 | Reader fallback smoke | **pass** | After the real source dispatch exited, source `history --json`, `status --id --json`, and `wait --until done` all resolved `cx-smoke-090b` from canonical telemetry; wait returned accepted `done`. Redacted results and raw-output hashes are in `koder/releases/0.9.0-source-smoke.json`. |
| 6 | Phase/config gates | **pass** | Focused + full tests cover no config, allowlisted, warn, reject/no-row, first-class override, malformed JSON/shapes, and non-git behavior. Invalid retention env/config also rejects before spawn/rows. |
| 7 | Retention/doctor gates | **pass** | Synthetic-only tests cover age and oldest-first size pruning, current/registry/start-row protection, protected-over-cap, malformed live metadata, lock/throttle, bounded candidate preview, scope safety, doctor report/dry-run/apply, and pre-spawn validation. `HARNEX_STATE_DIR=<temp> harnex doctor --prune --dry-run` passed. Real user logs were not pruned during candidate verification. |
| 8 | Independent integrated review | **pass after two bounded fix cycles** | Initial review found no P0 code blocker and requested release evidence + two `fallback` doc fixes. First rereview required durable source-smoke evidence. Final fresh rereview approved with zero unresolved P0/P1/P2 after `koder/releases/0.9.0-source-smoke.json` landed and hashes/invariants passed. |
| 9 | Gem build and publish | **pass** | `gem build harnex.gemspec` produced `harnex-0.9.0.gem` (SHA-256 `cd82d12a0f1646070da42e131a7c4c82af57cb0ebdfca0ea916227050108b10b`); `bin/gem-push harnex-0.9.0.gem` registered `harnex (0.9.0)` on RubyGems. |
| 10 | Git release publication | **pass** | Annotated `v0.9.0` at `abcbc1b`; pushed `main` and tag to origin. |
| 11 | Local installed-binary verification | **pass** | `gem install harnex`; `harnex --version` returned `harnex 0.9.0 (2026-08-03)`. Installed real Codex smoke wrote exactly two v2 rows with accepted proof, `gpt-5.5`/flex, context peak 12,203, `cost_usd=0.085113`, mirror equality, reader fallback, doctor dry-run, and bundled docs. Evidence: `koder/releases/0.9.0-installed-smoke.json` (SHA-256 `eb4cb781d4e4fbfc33d6034c4789d4c182a22d7a5ccba01da2cbb4965168bd86`). |
| 12 | Artifact cleanup | **pass** | Removed local `harnex-0.9.0.gem` after installed verification. |

## Migration evidence

- Consumers must stop redirecting `--summary-out` merely to retain rich rows;
  tracked `.harnex/dispatch.jsonl` is canonical.
- Holm imported all 1,156 valid legacy rich scratch rows into its tracked
  stream without modifying/deleting the scratch source; queue/session rollups
  now prefer v2 `usage` fields, deduplicate mixed-era rows, and ignore starts.
- Mixed v1 thin, envelope-less rich, and v2 start/end rows are covered by tests.

## Long-run telemetry

No multi-hour or overnight workload was run. Candidate work used bounded audit,
implementation, and three independent review/rereview dispatches. The real
source smoke lasted 34 seconds; the installed smoke was another short real
`gpt-5.5` turn. The first source-smoke failure was a caller-shell credential
environment mismatch, not a transport disconnect or source defect; the one
corrected attempt succeeded. Source and installed selected telemetry are in the
two bounded JSON evidence artifacts linked above.

## What this verification did NOT cover

- Deleting real user events/output logs. Retention was exercised only against
  disposable `HARNEX_STATE_DIR` trees; the installed default will prune
  opportunistically on a future dispatch.
- A `gpt-5.5` turn at or above 272K active context. Harnex deliberately leaves
  price-table cost null there until exact long-context rates can be represented.
- Claude structured usage/cache-write capture or Claude cost (#58 remains open).
- A live three-dispatch retry/fallback chain; chain derivation is deterministic
  fixture coverage only.
- Multi-hour queue workload, macOS/other cross-platform behavior, or #64
  observed-state receipts / #57/#59 deterministic queue mechanics.
